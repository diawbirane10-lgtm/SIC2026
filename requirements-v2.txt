# FLOWTRUST-AFR V2 training environment (GPU runner)
# Pin exact versions in lock file after first successful GPU run.
torch>=2.7,<3
torchvision>=0.22
transformers>=4.56
accelerate>=1.2
peft>=0.15
datasets>=3.5
safetensors>=0.5
huggingface-hub>=0.30
chronos-forecasting>=2.2.0
pandas>=2.2
pyarrow>=18
numpy>=2.0,<3
scipy>=1.14
scikit-learn>=1.6
joblib>=1.4
opencv-python-headless>=4.10
albumentations>=2.0
Pillow>=11
timm>=1.0.20
matplotlib>=3.9
soundfile>=0.13
librosa>=0.11
dasheng>=0.1
pytest>=8,<9

# DINOv3 backbones are supported by released Transformers >=4.56.0.
# Official DINOv3/SAM weights retain their respective upstream licenses and
# are never committed into this repository.
