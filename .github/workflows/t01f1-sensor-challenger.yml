name: FLOWTRUST T01-F1 sensor challenger

on:
  push:
    branches: [v2-model-rebuild]
    paths:
      - '.github/workflows/t01f1-sensor-challenger.yml'
      - 'v2/training/t01_sensor_classical.py'
      - 'v2/training/build_t01f_uci_sensor_dataset.py'
  workflow_dispatch:

jobs:
  sensor-challenger:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - name: Checkout V2 branch
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install 'numpy>=2,<3' 'scikit-learn>=1.6,<1.8' joblib

      - name: Download UCI hydraulic real sensor data
        run: |
          mkdir -p data/uci
          curl -L --fail --retry 5 --retry-delay 10 --max-time 1200 \
            -o uci_hydraulic.zip \
            'https://archive.ics.uci.edu/static/public/447/condition%2Bmonitoring%2Bof%2Bhydraulic%2Bsystems.zip'
          sha256sum uci_hydraulic.zip | tee uci_hydraulic.sha256
          unzip -q uci_hydraulic.zip -d data/uci

      - name: Locate UCI dataset root
        id: uci
        shell: bash
        run: |
          PS1=$(find data/uci -type f -name PS1.txt -print -quit)
          test -n "$PS1" || { echo 'PS1.txt not found' >&2; exit 1; }
          ROOT=$(dirname "$PS1")
          echo "root=$ROOT" >> "$GITHUB_OUTPUT"
          echo "UCI root: $ROOT"

      - name: Build 600-cycle group-disjoint fault benchmark
        run: |
          python v2/training/build_t01f_uci_sensor_dataset.py \
            --root '${{ steps.uci.outputs.root }}' \
            --out data/t01f1_uci_sensor_faults.npz \
            --max-cycles 600 \
            --seed 2026

      - name: Run sensor-integrity challengers
        run: |
          mkdir -p artifacts/t01f1
          /usr/bin/time -v python v2/training/t01_sensor_classical.py \
            --data data/t01f1_uci_sensor_faults.npz \
            --outdir artifacts/t01f1 \
            --seed 2026 \
            2>&1 | tee artifacts/t01f1/run.log

      - name: Record provenance
        if: always()
        run: |
          mkdir -p artifacts/t01f1
          cp uci_hydraulic.sha256 artifacts/t01f1/ 2>/dev/null || true
          python -m pip freeze > artifacts/t01f1/pip_freeze.txt
          printf 'github_sha=%s\n' "$GITHUB_SHA" > artifacts/t01f1/runtime.txt
          if [ -f artifacts/t01f1/metrics.json ]; then cat artifacts/t01f1/metrics.json; fi

      - name: Upload T01-F1 evidence bundle
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: FLOWTRUST_T01F1_${{ github.sha }}
          path: |
            artifacts/t01f1/metrics.json
            artifacts/t01f1/closed_test_outputs.npz
            artifacts/t01f1/sensor_integrity_head.joblib
            artifacts/t01f1/run.log
            artifacts/t01f1/runtime.txt
            artifacts/t01f1/pip_freeze.txt
            artifacts/t01f1/*.sha256
          if-no-files-found: warn
          retention-days: 14
